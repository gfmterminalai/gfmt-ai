{
  "version": 2,
  "crons": [{
    "path": "/api/sync",
    "schedule": "0 * * * *"
  }],
  "functions": {
    "api/sync.ts": {
      "maxDuration": 60
    }
  },
  "buildCommand": "rm -rf node_modules && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "dist"
} 